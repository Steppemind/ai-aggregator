// 🚀 Сгенерировано GPT через API — 2025-04-08

'use client'

import React from 'react'

export default function GPTButton({ onClick }: { onClick?: () => void }) {
  return (
    <button
      onClick={onClick}
      className="bg-indigo-600 text-white px-4 py-2 rounded hover:bg-indigo-700 transition"
    >
      🔮 GPT Кнопка
    </button>
  )
}